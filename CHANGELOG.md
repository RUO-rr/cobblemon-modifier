# 更新日志

本项目沿用语义化版本。`2.x` 系列未公开发布，因此这一版直接进入 **3.0.0**。

---

## 3.0.0 — 从桌面程序到游戏内模组

这是一次形态上的大版本：项目从独立 Swing 程序完整迁移为 **Fabric 客户端模组**，
并在上线后按玩家反馈完成多轮迭代。架构演进与技术决策详见 [ARCHITECTURE.md](ARCHITECTURE.md)。

### 新能力

- **游戏内界面**：按 `K` 打开修改器；插件切换、宝可梦列表、字段编辑、进度与日志全部在游戏内完成
- **数据源补全**：除 `mods/*.jar` 外，还会读取整合包的 `global_packs/required_data`、`resourcepacks`、
  `global_packs/jostar_overrides`，并按 `level.dat` 的真实加载顺序确定优先级
- **species_additions 支持**：数据包用于整体覆盖物种字段的 `species_additions` 文件会出现在列表里，可直接编辑
- **扇出写入**：改动会同步写进该宝可梦的**所有**来源文件（本体 + 每个覆盖文件），
  无论游戏内部按什么顺序应用覆盖，生效的都是你的值；`还原原版` 会一并清理这些覆盖
- **技能修改**：新增"技能修改"插件（新增 / 改等级 / 删除招式），
  并基于 1386 条已知招式表做输入校验
- **多 Mega 开关**：一键解除"同时只能有一只 Mega"的限制（需要 mega_showdown），立即生效
- **字段查找**：按图鉴编号 / 名字 / 特性搜索并跳转到对应宝可梦

### 修复

- **修复形态数据被写坏（严重）**：形态没有 `baseStats` 时本应"继承本体"，
  旧实现会写入一串 `0`，导致 223 个形态（涉及 105 个物种）在对战里变得极弱；
  同类问题在特性插件中也已修复
- 修复"还原原版"只删除一份覆盖、导致半还原状态的问题
- 修复同名数据包在 `resourcepacks` 与 `global_packs` 各有一份时的重复扫描与重复写入
- 修复"不是宝可梦的数据"（图鉴条目等 1214 条）被误收进列表的问题

### 性能

- 扫描改为"一个压缩包只打开一次 + 批量读取"：实测 **35 ~ 126 秒 → 0.41 秒**

### 移除

- 移除"单只宝可梦刷新率编辑"（数据包覆盖机制在该类整合包中无法稳定生效，仅保留全局稀有等级权重）
- 移除模组内置的 `logback.xml`（Fabric 会覆盖它，留着只会产生误导性的空日志文件）
- 移除桌面版遗留的死代码与未使用资源

### 工程

- 254 个单元测试全部通过；接入 GitHub Actions 自动构建与测试
- `fabric.mod.json` 标注 `environment: client`，并声明 `recommends: mega_showdown`
- 许可证改为 MIT

---

## 2.0.0（未公开发布）— 桌面版分层改造

- **MVC 拆分**：`MainFrame` 从 1643 行缩减到 554 行，拆出 controller / service / repository 三层
- **DTO 提取**：用 `PokemonStats`、`FormInfo`、`StatField`、`JarResourcePath` 取代 `Map<String, Object>` 与散落的内部类
- **依赖注入**：`ModifierContainer` 作为组合根，插件改用 `ServiceLoader` 自动发现
- **测试**：补齐 147 个单元测试
- **收尾**：接入 SLF4J、清理死代码、统一字体与线程池

## 1.x — 桌面版

- 扫描 `mods` 目录下的模组 JAR，直接定位并修改宝可梦 JSON
- 提供种族值 / 特性 / 刷新率等编辑插件
